---
import '../styles/global.css';

const { title } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title ? `${title} | OpenTab` : 'OpenTab'}</title>
  </head>
  <body>
    <header class="site-header">
      <div class="container">
        <a class="logo" href={`${Astro.base}`}>OpenTab</a>
        <nav class="nav">
          <a href={`${Astro.base}`}>Home</a>
          <a href={`${Astro.base}spec`}>Spec</a>
          <a href={`${Astro.base}examples`}>Examples</a>
          <a href={`${Astro.base}roadmap`}>Roadmap</a>
        </nav>
      </div>
    </header>
    <main class="container">
      <slot />
    </main>
  </body>
</html>
